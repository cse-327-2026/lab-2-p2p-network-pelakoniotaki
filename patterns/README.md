# Pattern spotlights

These are short demos to connect DS concepts to real engineering patterns.

- `sidecar_log_shipper/` — sidecar container tails app logs
- `grpc_interceptors/` — middleware/interceptors for request IDs and timing
